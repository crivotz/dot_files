gui:
  showIcons: true
git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never --24-bit-color=never
customCommands:
  - key: 'I'
    command: "gh issue list"
    context: 'global'
    subprocess: true
    description: "Issue aperte"
